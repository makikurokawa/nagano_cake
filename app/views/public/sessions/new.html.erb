<div class="container">
  <div class="row">
    <h4 class="my-5 bg-light text-center col-2">ログイン</h4>
  </div>
  <div class="row">
    <p class="ml-5 text-center bg-light col-4">会員の方はこちらからログイン</p>
  </div>
  <div class="form-group mx-auto">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="row my-3">
        <%= f.label "メールアドレス", class: "col-2 ml-5" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control col-6", placeholder: "sample@example.com" %>
      </div>
      <div class="row mb-3">
        <%= f.label "パスワード", class: "col-2 ml-5" %>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control col-6" %>
      </div>
      <div class="row my-5 ml-5">
        <%= f.submit "ログイン", class: "btn btn-primary btn-md px-5 ml-5" %>
      </div>
    <% end %>
    <div class="row">
      <p class="ml-5 text-center bg-light col-4">会員登録がお済みでない方</p>
    </div>
    <div class="row">
      <%= render "public/shared/links" %>
      <p>から新規登録を行ってください。</p>
    </div>
  </div>
</div>